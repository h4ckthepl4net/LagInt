package main.body.header;

import classes.common.enums.Language;
import classes.common.enums.Theme;

public class header {
    public Language language = Language.ARMENIAN;
    public Theme theme = Theme.LIGHT;
}
